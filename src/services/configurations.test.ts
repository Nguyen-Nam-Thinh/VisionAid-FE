import { expect,it } from 'vitest';
import { createMockService } from './mocks/service';
it('validates config range and records rollback as a new revision',async()=>{const s=createMockService(undefined,0);await s.login('admin@demo.vn','Demo@123');const e=(await s.snapshot()).entities.configs[0];await expect(s.execute({type:'save',kind:'configs',entity:{...e,fields:{...e.fields,value:1000}}})).rejects.toThrow('giới hạn');await s.execute({type:'save',kind:'configs',entity:{...e,fields:{...e.fields,value:20}}});let db=await s.snapshot();expect(db.revisions).toHaveLength(1);await s.execute({type:'rollback',revisionId:db.revisions[0].id});db=await s.snapshot();expect(db.entities.configs[0].fields.value).toBe(15);expect(db.revisions).toHaveLength(2);expect(db.audit[0].action).toContain('Khôi phục');});
